echo "Loading basic ubuntu-conda environment"

# this defaults to ia64, but we will use gfortran, not ifort
export GFDL_MKMF_TEMPLATE=mac_conda

export F90=mpifort
export CC=mpicc
export CDEFS=" -DMACOS "